<h1>
  <%=current_user.username.titlecase%>'s Wishlist 
</h1>

<hr>

<table class="table">
  <tr>

    <th>
      Product
    </th>


    <th>
    </th>
  </tr>

  <% Like.where({ :user_id => current_user.id}).each do |like| %>
  
  <tr>
    <td>
      <%= Product.find(like.product_id).full_product %>
    </td>
    
    <td>
        Test
    </td>
  </tr>
  <% end %>
</table>
